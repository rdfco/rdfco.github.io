# Services

External-system adapters and application services. Services do not render UI and must expose typed, replaceable boundaries rather than leaking vendor SDKs through the application.

No service is introduced by M2.
