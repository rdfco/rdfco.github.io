# Pages

Route-level composition and page metadata. Pages assemble layouts and features but do not own reusable domain implementation, shared UI primitives, or low-level Three.js systems.

Each future page owns a folder and a small public `index.ts`. Migration begins in M6.
