import { z } from 'zod'
export const cardSchema = z.object({ title: z.string().min(1), text: z.string().min(1) })
export const navigationItemSchema = z.object({ label:z.string().min(1), href:z.string(), enabled:z.boolean(), showInMenu:z.boolean(), active:z.boolean().optional() })
export const siteContentSchema = z.object({ seo:z.object({title:z.string().min(1),description:z.string().min(1)}), navigation:z.array(navigationItemSchema), hero:z.object({title:z.string().min(1),subtitle:z.string().min(1),scrollLabel:z.string()}), introduction:z.object({title:z.string(),body:z.string()}), faraSections:z.object({solutions:z.array(cardSchema),ai:cardSchema.extend({subtitle:z.string()}),industries:z.array(cardSchema)}),footer:z.object({eyebrow:z.string(),title:z.string(),copyright:z.string(),caseStudies:z.array(z.string())}) })
