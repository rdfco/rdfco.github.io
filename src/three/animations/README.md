# Three.js animations

Shared AnimationMixer lifecycle, clip binding, and 3D animation utilities. DOM/GSAP animation infrastructure belongs in `src/animations`; scene-specific timelines remain inside their scene.
