# Models

Typed GLB definitions, loader policy, node contracts, transforms, preload, compression, LOD, animations, and fallback metadata. GLB binaries remain under `public/assets/models`.

Model registry and loader exports are introduced in M9.
