# Stores

Cross-feature client state only. Component-local and feature-local state stays with its owner; per-frame scene state must not cause React renders every frame.

The current `src/store/experience.ts` remains in place until its owning migration milestone.
