# App

Application bootstrap, global providers, and top-level error boundaries. App may compose routes and infrastructure but must not own page content, feature behavior, or scene implementation.

Public imports will be exposed from `index.ts`. Runtime migration begins in M6; M2 contains no active implementation here.
