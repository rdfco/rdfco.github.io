# Three types

Cross-domain contracts used by multiple Three.js foundation modules, including camera,
DPR, fog, renderer, runtime, and performance-tier definitions.

Types owned by one scene, model, material, loader, or camera implementation remain
colocated with that future module instead of growing this folder into a global barrel.
