# Asset definitions

Typed runtime asset metadata, ownership, provenance, lifecycle, and general lookup. Binary files remain under `public/assets`; this folder never stores GLB, image, texture, font, audio, or video binaries.

Current definitions remain in `src/data/assetRegistry.ts` until M3.
