import { mkdir, writeFile } from 'node:fs/promises'
import puppeteer from 'puppeteer-core'
const output=process.env.OUTPUT_DIR||'work/contracts/native',url=process.env.SITE_URL||'http://127.0.0.1:4174/native-preview'
await mkdir(output,{recursive:true});const browser=await puppeteer.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--no-sandbox','--disable-gpu-sandbox']});const report={}
for(const viewport of [{name:'desktop',width:1440,height:900},{name:'tablet',width:768,height:1024},{name:'mobile',width:390,height:844}]){const page=await browser.newPage(),errors=[];page.on('console',message=>{if(message.type()==='error')errors.push(message.text())});page.on('pageerror',error=>errors.push(error.message));page.on('response',response=>{if(response.status()>=400)errors.push(`${response.status()} ${response.url()}`)});await page.setViewport({width:viewport.width,height:viewport.height,deviceScaleFactor:1});await page.goto(url,{waitUntil:'networkidle0',timeout:60000});await new Promise(resolve=>setTimeout(resolve,2500));const height=await page.evaluate(()=>document.documentElement.scrollHeight);for(const [name,progress]of Object.entries({hero:0,quarter:.25,middle:.5,'three-quarter':.75,footer:1})){await page.evaluate(value=>scrollTo(0,(document.documentElement.scrollHeight-innerHeight)*value),progress);await new Promise(resolve=>setTimeout(resolve,900));await page.screenshot({path:`${output}/${viewport.name}-${name}.png`})}report[viewport.name]={height,errors};await page.close()}
await writeFile(`${output}/contract.json`,JSON.stringify(report,null,2));await browser.close();if(Object.values(report).some(result=>result.errors.length))process.exitCode=1
