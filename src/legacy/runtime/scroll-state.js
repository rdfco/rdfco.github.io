// The one piece of scroll state the navigation highlighting also has to read:
// a programmatic section scroll must not be overridden by the listener that
// follows the visitor's own scroll position.

export const scrollState = { programmaticSectionScroll: false }
