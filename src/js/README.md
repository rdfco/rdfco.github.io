# Legacy customization runtime

Imperative DOM customization executed inside the protected legacy iframe. This is a transitional adapter copied to `dist/src` by Vite. Native modules must not import it; removal occurs only in M17.
