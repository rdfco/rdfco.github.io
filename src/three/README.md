# Three.js and R3F

The native 3D subsystem. R3F owns Canvas and scene lifecycle; React page code consumes scene public APIs rather than scene internals.

Current implementation remains under `src/native` and `src/scenes` until M8–M12. New shared code requires a real consumer and the approved milestone.
