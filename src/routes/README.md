# Routes

Route declarations, guards, lazy boundaries, and route metadata. Route modules select pages; they do not implement page UI or business features.

Public routing APIs will be exposed from `index.ts` in M6. Current routing remains in `src/App.tsx` until then.
