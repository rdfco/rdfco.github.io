# Current data boundary

Current mixed content, validation, footer, and asset definitions used by Legacy adapters and Native Preview. M3–M5 migrate these responsibilities into `src/assets`, `src/content`, and `src/config` without changing consumers all at once.
