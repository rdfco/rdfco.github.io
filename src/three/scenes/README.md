# Scenes

Colocated scene modules and the typed Scene Registry. A scene folder owns its composition, config, asset bindings, camera/animation settings, private materials/shaders, tests, and README.

The registry is introduced in M8; current scene files are not moved in M2.
