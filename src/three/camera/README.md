# Camera

Camera rigs, curves, targets, interpolation, and progress mapping. Scene-specific camera configuration stays inside its scene and consumes these shared contracts.

Camera system exports are introduced in M10.
