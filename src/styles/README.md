# Current legacy styles

CSS modules imported by the legacy customization entry point. These styles are transitional and selector-sensitive. Target global tokens and shared style layers will be classified separately; M2 does not move or reorder CSS.
