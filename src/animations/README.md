# DOM animations

Shared GSAP/DOM animation lifecycle, reusable presets, ScrollTrigger policy, and reduced-motion behavior. Feature-specific animations remain inside their feature; Three.js animation mixers belong under `src/three/animations`.

Shared animation exports are introduced in M12.
