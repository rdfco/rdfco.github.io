# FARA navigation

`navigation.js` is the single source of truth for navbar/menu routes.
`navigation-events.js` safely hands iframe navigation to React Router.
Each route owns its content under `pages/<route>/`.
